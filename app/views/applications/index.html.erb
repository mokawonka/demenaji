<h2 class="title">
  Mes demandes
  <%= image_tag 'img_apps.png', class: 'mt-3 mb-5 float-right' %>
</h2>

<% if @applications.empty? %>
  <div class="container watermark">
    <span class="fs-20">Vous n'avez postulé pour aucune annonce pour l'instant.</span>
  </div>

<% else %>
  <form method="post">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>

    <table class="table">
      <tbody>
        <% @applications.each do |item| %>
          <tr class="bluerow">
            <td class="text-center fs-18">
              <%= (item.creation_time || item.created_at).strftime('%B %d, %Y') %>
            </td>

            <td class="text-center fs-18">
              <% if item.place %>
                <a href="/Place/<%= item.place.id %>"><b><%= item.place.address %></b></a>
              <% else %>
                <i class="fs-17">Annonce supprimée</i>
              <% end %>
            </td>

            <td class="text-center">
              <span class="badge badge-primary">
                <%= case item.status
                    when 0, -2 then 'Refusé'
                    when 1     then 'Invité'
                    else            'En cours'
                    end %>
              </span>
            </td>

            <td class="text-center">
              <button formaction="/MyApplications/Delete/<%= item.id %>"
                      class="btn btn-sm btn-danger">Supprimer</button>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </form>
<% end %>